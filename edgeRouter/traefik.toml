[api]
  insecure = true
  dashboard = true
  debug = true

[entryPoints]
  [entryPoints.etcd]
    address = ":2379"
  [entryPoints.vault]
    address = ":443"

[providers.file]
    directory = "/etc/traefik"
    watch = true
    filename = "routes.yml"
    debugLogGeneratedTemplate = true